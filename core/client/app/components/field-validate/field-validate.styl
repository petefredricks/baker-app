@require '../../common/mixins.styl';
@require 'nib';

.error-message
	color errorRed
	display none
	font-size 12px
	padding 6px 0

.validation-error
	input, select
		border-color errorRed

	.error-message
		display block